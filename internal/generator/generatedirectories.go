package generator

import "github.com/ndk123-web/trak/internal/models"

func GenerateDirectories(toolTemplate *models.ToolTemplateModel) {
	// efficiently generating directories and files recursively
}
